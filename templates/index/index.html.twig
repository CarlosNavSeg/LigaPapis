{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block body %}
    <div class="wrapper">
        <div class="row landingImage">
            <div class="col-6 title">
                <h1>
                    Bienvenido a LigaPapis
                </h1>
                <span> La mejor liga del mundo.</span>
            </div>
            <div class="col-6 description">
                <p>
                    En está página podrás encontrar información sobre la liga de papis y sus competidores.
                </p>
            </div>
        </div>
        <div class="row teamNewsTable">
            <div class="col-8 teamTable">
                {% for papi in papis %}                    
                <div class="row teamMember">
                    <div class="col-6 avatar">
                        <img src="{{asset('papi_avatar/' ~ papi.User.Avatar) }}" alt="papiAvatar" class="img-thumbnail img-fluid width:200px h-200"/>
                    </div>
                    <div class="col-6 shortDescription">
                        <h2 class="username">
                            {{papi.User.Nombre}}
                        </h2>
                        <p class="memberMessage">
                            {{papi.mensaje}}
                        </p>
                    </div>
                </div>
                {% endfor %}
            </div>
            <div class="col-4 publicacionTable">
            {% for publi in publis %}
                <div class="publicacion">
                    <img src={{asset('publicaction_images/' ~ publi.Image) }} alt="imagen publi" class="img-responsive"/>
                    <p>{{ publi.PieDeFoto}}</p>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
